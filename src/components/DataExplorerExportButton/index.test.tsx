describe("Accessibility", () => {
  it.todo("should have no accessibility violations");

  it.todo("should have no accessibility violations when disabled");
});

describe("Basic Functionality", () => {
  it.todo("should render without crashing");

  it.todo("should gracefully handle API errors (GraphQL)");

  it.todo("should gracefully handle API errors (Network)");

  it.todo("should gracefully handle API errors (API)");

  it.todo("should be disabled when `disabled` is true");

  it.todo("should be disabled when downloading the data");
});

describe("Implementation Requirements", () => {
  it.todo("should have a tooltip on the button");

  it.todo("should correctly name the exported TSV");

  it.todo("should only include the visible columns in the exported TSV");
});
